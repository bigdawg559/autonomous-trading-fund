console.log('🔄 Running backtest for strategies...');\n\nconst strategies = ['trend_following', 'mean_reversion', 'volatility_breakout'];\nconst mockData = Array.from({ length: 100 }, (_, i) => ({\n  close: 43000 + Math.random() * 1000,\n  volume: Math.random() * 1000,\n}));\n\nfor (const strategy of strategies) {\n  let portfolio = 10000;\n  let trades = 0;\n  let wins = 0;\n  \n  for (let i = 0; i < mockData.length - 1; i++) {\n    const change = (mockData[i + 1].close - mockData[i].close) / mockData[i].close;\n    if (Math.abs(change) > 0.001) {\n      const pnl = portfolio * 0.01 * change;\n      portfolio += pnl;\n      trades++;\n      if (pnl > 0) wins++;\n    }\n  }\n  \n  const winRate = (wins / trades * 100).toFixed(2);\n  const totalPnL = (portfolio - 10000).toFixed(2);\n  console.log(`✅ ${strategy}: Win Rate ${winRate}%, Trades: ${trades}, Total PnL: $${totalPnL}`);\n}