name: btc-signal-ci

on:
  pull_request:
    paths:
      - 'btc-signal/**'
  push:
    branches: [main]
    paths:
      - 'btc-signal/**'

jobs:
  python:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: btc-signal/backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - run: python -m pip install --upgrade pip
      - run: pip install -r requirements.txt pytest
      - run: python -m compileall btc_signal
      - run: pytest -q
