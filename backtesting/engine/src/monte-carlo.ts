console.log('🎲 Running Monte Carlo Simulation...');\n\nconst returns = Array.from({ length: 252 }, () => (Math.random() - 0.5) * 0.02);\nconst final_values: number[] = [];\n\nfor (let sim = 0; sim < 10000; sim++) {\n  let portfolio = 10000;\n  for (const r of returns) {\n    portfolio *= 1 + r;\n  }\n  final_values.push(portfolio);\n}\n\nfinal_values.sort((a, b) => a - b);\n\nconst p5 = final_values[Math.floor(final_values.length * 0.05)];\nconst p25 = final_values[Math.floor(final_values.length * 0.25)];\nconst median = final_values[Math.floor(final_values.length * 0.5)];\nconst p75 = final_values[Math.floor(final_values.length * 0.75)];\nconst p95 = final_values[Math.floor(final_values.length * 0.95)];\nconst probProfit = (final_values.filter(v => v > 10000).length / 10000 * 100).toFixed(2);\n\nconsole.log('\\nSimulation Results (1 year, $10k capital):');\nconsole.log(`   5th Percentile: $${p5.toFixed(2)}`);\nconsole.log(`   25th Percentile: $${p25.toFixed(2)}`);\nconsole.log(`   Median: $${median.toFixed(2)}`);\nconsole.log(`   75th Percentile: $${p75.toFixed(2)}`);\nconsole.log(`   95th Percentile: $${p95.toFixed(2)}`);\nconsole.log(`   Probability of Profit: ${probProfit}%`);