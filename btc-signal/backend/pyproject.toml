[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "btc-signal-engine"
version = "0.1.0"
description = "BTCUSDT 15-minute adaptive signal engine foundation"
requires-python = ">=3.13"
